Approved: the corrected contract accurately limits current log-api evidence to the single-record operation-kind case, preserves W2 evidence as pending, and correctly assigns move projection and recovery to memory-kernel and opaque OperationJournal envelope persistence and query to log-api. The corrected persisted path is `<root>/<workspace_slug>/journals/<journal_id>.json`. This approval is independent of the authoring pass and confirms the contract is suitable as the W1 prerequisite for downstream ticketing and implementation planning. W2 remains responsible for full-field multi-record round-trip, combined-filter, and deterministic-ordering evidence.