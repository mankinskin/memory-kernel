Approved 2026-06-15. These decisions supersede any conflicting clause above (notably H4).

- **Q6.1 / QX.1 Scaffold lands now, in this ticket.** `52dfd793` lands the guarded-no-op store-index branch in `.githooks/pre-commit` as part of its own implementation, before any generator. Each generator ticket then flips its domain from no-op to active via a one-line change. `done` for `52dfd793` requires both the contract spec AND the scaffolded hook block (all domain flags off); a spec-only close under-delivers the "define and implement" scope.
- **Q6.2 `--check`-only model (supersedes H4).** The hook runs `<domain> store-index --check` (read-only). On drift it prints the touched outputs + a restage instruction and exits non-zero; it does NOT auto-write. The user runs the writing variant (`<domain> store-index regen`) and restages. This matches the existing `rule sync-targets --check` behavior in the same hook and avoids racing the user's staged content. H4 is amended accordingly.
- **Q6.3 Audit trigger is exempt by default.** The blanket `^\.audit/` trigger is removed. The audit index is derived from an audit run, not from editing `.audit/`. Audit defaults to hook-exempt / post-commit (or manual regen after an audit run) until `855a1e5d` defines its real source inputs; H2 marks audit as "trigger TBD, exempt by default."